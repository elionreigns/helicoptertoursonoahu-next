import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  // Use "npm run build" (with --webpack in package.json) to avoid Turbopack hang; no experimental.turbopack key in Next 16
};

export default nextConfig;
